/**
 * BAi — entry point.
 *
 * Stage 0: scaffold only. The real work starts in Stage 1, where this file
 * will wire up the first agent adapter.
 */

function main(): void {
  console.log('BAi — multi-agent collaboration platform (stage 0 scaffold)');
}

main();
